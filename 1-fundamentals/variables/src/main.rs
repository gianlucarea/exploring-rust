fn main() {
    let bunnies: i32 = 4; // Assign with value and type
    let (potato, carrots) = (4,15); // Multiple assign 
    let mut mutable_bunnies = 16;
    mutable_bunnies = 4;
    const CONST_BUNNIES : u32 = 10; //Constant must annotate type  
}
